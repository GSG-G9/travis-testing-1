/* eslint-disable no-undef */
// just test jest :)

describe('Test all of pure function', () => {
	test('first test', () => {
		const expected = 2;
		const acutal = 2;
		expect(expected).toBe(acutal);
	});
});
